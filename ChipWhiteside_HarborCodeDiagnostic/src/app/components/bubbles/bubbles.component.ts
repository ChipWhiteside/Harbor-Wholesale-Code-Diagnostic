import { Component } from '@angular/core';

@Component({
  selector: 'app-bubbles',
  templateUrl: './bubbles.component.html',
  styleUrls: ['./bubbles.component.css']
})
export class BubblesComponent {
  // Work in progress, return later
  // Need to get mouse position on screen...

  // wrapper = document.getElementById("bubble-wrapper");
  
  // animateBubble(x:number) {
  //   bubble = document.createElement("div");
  //   bubble.className = "bubble";
  //   bubble.style.left = '${x}px';
  //   wrapper.appendChild(bubble);
  //   setTimeout(() => wrapper.removeChild(bubble), 2000);
  // }

  // document.onmousemove = (e: number) => this.animateBubble(e.clientX);


}
